pipeline{
    agent{
        docker {
            image 'python:3.11.9-alpine'
        }
    }
    environment {
        PATH = "/usr/local/bin:${env.PATH}"
    }
    stages {
        stage('Test') {
            steps {
                sh 'python3 --version'
            }
        }
    }
}